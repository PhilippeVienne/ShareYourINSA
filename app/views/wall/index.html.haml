.container
  .margin
    .row
      .col-sm-3
        .well.text-center.margin
          %h3 Mon Profile
          = image_tag current_user.profile_user.avatar.url, class: %w{image-rounded margin}, width: '80%'
          = link_to 'Editer', :edit_profile
        .margin.text-center
          = link_to 'INSA Lyon', 'https://www.insa-lyon.fr'
      .col-sm-9
        %form#new-post.margin
          .form-group
            %textarea.form-control{placeholder: 'Ecrire un post ...'}
          %btn.btn.btn-block.btn-default Poster
        - @posts.each do |post|
          = render partial: 'post', locals: {post: post}